# Backend (Payload CMS) dev image — serves admin + API on :3001
FROM node:20-alpine AS base
ENV NODE_ENV development
WORKDIR /app

# Dependency + layer caching
COPY package.json ./
RUN npm install

# App source
COPY . .

EXPOSE 3001
CMD ["npm", "run", "dev"]
